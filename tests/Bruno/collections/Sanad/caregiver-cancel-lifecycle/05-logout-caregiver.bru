meta {
  name: 05-logout-caregiver
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/auth/sessions/logout
  body: none
  auth: bearer
}

headers {
  X-Device-Session-Id: {{cgLifecycleDeviceSessionId}}
}

auth:bearer {
  token: {{jwt}}
}

assert {
  res.status: eq 204
}
