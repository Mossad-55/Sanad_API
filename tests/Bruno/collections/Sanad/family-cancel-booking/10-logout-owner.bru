meta {
  name: 10-logout-owner
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/api/v1/auth/sessions/logout
  body: none
  auth: bearer
}

headers {
  X-Device-Session-Id: {{familyDeviceSessionId}}
}

auth:bearer {
  token: {{jwt}}
}

assert {
  res.status: eq 204
}
