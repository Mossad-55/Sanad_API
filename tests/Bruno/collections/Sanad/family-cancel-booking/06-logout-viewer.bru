meta {
  name: 06-logout-viewer
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/auth/sessions/logout
  body: none
  auth: bearer
}

headers {
  X-Device-Session-Id: {{viewerDeviceSessionId}}
}

auth:bearer {
  token: {{jwtViewer}}
}

assert {
  res.status: eq 204
}
