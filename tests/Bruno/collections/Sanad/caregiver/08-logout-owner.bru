meta {
  name: 08-logout-owner
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/auth/sessions/logout
  body: none
  auth: bearer
}

headers {
  X-Device-Session-Id: {{caregiverFamilyDeviceSessionId}}
}

auth:bearer {
  token: {{jwtFamily}}
}

assert {
  res.status: eq 204
}
