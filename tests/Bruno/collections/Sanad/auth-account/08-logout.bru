meta {
  name: 08-logout
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/api/v1/auth/sessions/logout
  body: none
  auth: bearer
}

headers {
  X-Device-Session-Id: {{authAccountDeviceSessionId}}
}

auth:bearer {
  token: {{jwt}}
}

assert {
  res.status: eq 204
}
