meta {
  name: 11-logout-owner
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/api/v1/auth/sessions/logout
  body: none
  auth: bearer
}

headers {
  X-Device-Session-Id: {{familyDeviceSessionId}}
}

auth:bearer {
  token: {{jwt}}
}

assert {
  res.status: eq 204
}
