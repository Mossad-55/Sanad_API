meta {
  name: 01-login-admin
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{adminEmail}}",
    "password": "{{adminPassword}}",
    "deviceName": "bruno-subscription-admin-reads",
    "devicePlatform": 3,
    "appVersion": "1.0.0"
  }
}

assert {
  res.status: eq 200
}

script:post-response {
  if (res.status == 200) {
    bru.setVar("subscriptionAdminJwt", res.body.accessToken);
    bru.setVar("subscriptionAdminDeviceSessionId", res.body.deviceSessionId.value);
  }
}
