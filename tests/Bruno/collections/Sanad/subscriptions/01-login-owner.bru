meta {
  name: 01-login-owner
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{familyOwnerEmail}}",
    "password": "{{familyOwnerPassword}}",
    "deviceName": "bruno-subscriptions",
    "devicePlatform": 3,
    "appVersion": "1.0.0"
  }
}

assert {
  res.status: eq 200
}

script:post-response {
  bru.setVar("subscriptionOwnerJwt", res.body.accessToken);
  bru.setVar("subscriptionOwnerDeviceSessionId", res.body.deviceSessionId.value);
}
