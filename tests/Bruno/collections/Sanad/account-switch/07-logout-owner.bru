meta {
  name: 07-logout-owner
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/v1/auth/sessions/logout
  body: none
  auth: bearer
}

headers {
  X-Device-Session-Id: {{accountSwitchDeviceSessionId}}
}

auth:bearer {
  token: {{jwt}}
}

assert {
  res.status: eq 204
}
